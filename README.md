My portfolio is built with React.

GitHub pages works via `gh-pages` branch, even though it works from `master`.
How `gh-pages` works 🤷🏻, but the last time removing old branches helped to "refresh" it.

